# 🔍 Анализ дублирования кода

**Ветка:** `refactor/remove-code-duplication`  
**Дата:** 2025-01-XX

---

## 📊 Обнаруженные паттерны дублирования

### 1. **Обработка ошибок** (Критично - ~15+ мест)

**Паттерн:**
```typescript
try {
  // код
} catch (err) {
  console.error('❌ Ошибка при ...:', err);
  console.error('❌ Тип ошибки:', typeof err);
  console.error('❌ Сообщение ошибки:', err instanceof Error ? err.message : 'Неизвестная ошибка');
  error.value = `Ошибка при ...: ${err instanceof Error ? err.message : 'Неизвестная ошибка'}`;
  // или
  $q.notify({
    color: 'negative',
    message: error.message || 'Не удалось ...',
    icon: 'error',
  });
}
```

**Места:**
- `src/stores/roadmap-store.ts` - addItem, updateItem
- `src/pages/RoadmapPage.vue` - saveItem, deleteItem
- `src/pages/SeedlingTrayListPage.vue` - handleSave, confirmDelete
- `src/pages/VarietyLibraryV2Page.vue` - saveEdit
- `src/stores/variety-library.ts` - addVariety, updateVariety, deleteVariety
- `src/stores/pepper-firestore.ts` - addPepper, updatePepper
- И другие...

**Решение:** Создать `useErrorHandler` composable

---

### 2. **Уведомления Quasar** (Высокий приоритет - ~30+ мест)

**Паттерн:**
```typescript
$q.notify({
  color: 'positive' | 'negative' | 'warning' | 'info',
  message: '...',
  icon: 'check_circle' | 'error' | 'info' | 'warning',
  timeout: 2000 | 3000 | 5000,
});
```

**Места:**
- Повсеместно в компонентах и страницах
- Одинаковые паттерны для успеха/ошибки/предупреждения

**Решение:** Создать `useNotifications` composable

---

### 3. **Валидация форм** (Средний приоритет - ~10+ мест)

**Паттерн:**
```typescript
if (!form.value.field) {
  $q.notify({
    color: 'negative',
    message: 'Поле обязательно',
    icon: 'error',
  });
  return;
}
```

**Места:**
- `src/components/HistoryEntryDialog.vue` - saveEntry
- `src/components/PepperForm.vue` - validateForm
- `src/components/SeedlingTrayFormDialog.vue` - validateForm
- И другие формы...

**Решение:** Создать `useFormValidation` composable

---

### 4. **Работа с Firestore** (Средний приоритет - ~20+ мест)

**Паттерн:**
```typescript
const addItem = async (item: T) => {
  loading.value = true;
  error.value = null;
  try {
    const now = new Date().toISOString();
    const data = {
      ...item,
      createdAt: now,
      updatedAt: now,
    };
    await addDoc(collection(db, 'collection'), data);
  } catch (err) {
    error.value = 'Ошибка: ' + (err as Error).message;
    console.error('Error:', err);
    throw err;
  } finally {
    loading.value = false;
  }
};
```

**Места:**
- Все stores с CRUD операциями
- Одинаковая логика для add/update/delete

**Решение:** Создать утилиты для Firestore операций

---

### 5. **Логирование** (Низкий приоритет - ~50+ мест)

**Паттерн:**
```typescript
console.log('=== Начало ... ===');
console.log('Данные:', data);
console.log('✅ Успех');
console.error('❌ Ошибка:', error);
```

**Места:**
- Повсеместно в коде
- Особенно в `VarietyLibraryV2Page.vue`, `RoadmapPage.vue`

**Решение:** Создать систему логирования с уровнями

---

### 6. **Проверка авторизации** (Средний приоритет - ~10+ мест)

**Паттерн:**
```typescript
if (!userStore.user) {
  throw new Error('Только авторизованный пользователь может ...');
}
// или
ensureAuthenticated();
```

**Места:**
- Множество stores и компонентов

**Решение:** Уже частично решено через `ensureAuthenticated()`, но можно улучшить

---

## 🎯 План рефакторинга

### Фаза 1: Создание базовых composables

1. **`useErrorHandler.ts`** - централизованная обработка ошибок
2. **`useNotifications.ts`** - упрощенные уведомления
3. **`useLogger.ts`** - система логирования

### Фаза 2: Утилиты для Firestore

4. **`firestore-helpers.ts`** - общие функции для CRUD
5. **`firestore-types.ts`** - типы для Firestore операций

### Фаза 3: Улучшение форм

6. **`useFormValidation.ts`** - валидация форм
7. **`useFormState.ts`** - управление состоянием форм

### Фаза 4: Рефакторинг существующего кода

8. Заменить все использования на новые composables
9. Удалить дублирующийся код
10. Обновить типы

---

## 📝 Детальный анализ по файлам

### `src/pages/RoadmapPage.vue`
- ❌ Использование `alert()` вместо `$q.notify()`
- ❌ Дублирование обработки ошибок
- ❌ Избыточное логирование

### `src/stores/roadmap-store.ts`
- ❌ Дублирование обработки ошибок
- ❌ Избыточное логирование
- ❌ Повторяющаяся логика для add/update

### `src/pages/VarietyLibraryV2Page.vue`
- ❌ Множество `console.log` (30+)
- ❌ Дублирование обработки ошибок
- ❌ Использование `any` типов

### `src/components/HistoryEntryDialog.vue`
- ❌ Дублирование валидации
- ❌ Повторяющиеся паттерны уведомлений

### `src/stores/variety-library.ts`
- ❌ Дублирование CRUD операций
- ❌ Повторяющаяся обработка ошибок

---

## ✅ Ожидаемые результаты

После рефакторинга:
- **-30%** дублирующегося кода
- **+50%** переиспользуемости
- **+40%** читаемости
- **+60%** поддерживаемости
- Единообразная обработка ошибок
- Централизованное логирование
- Упрощенная валидация форм

